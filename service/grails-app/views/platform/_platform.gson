import org.olf.kb.Platform

import groovy.transform.*

@Field
Platform platform
json g.render(platform, [expand: ['locators']]) {  //, includes: ['id', 'name', 'localCode']
//  'locators' {'locators' platform.locators.domainName}    
}